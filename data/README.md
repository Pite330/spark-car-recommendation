# 数据目录

## raw

保存从公开数据集、课程资料或其他合法来源取得的原始汽车数据。原始文件只读保存，不在其上直接修改。大体积数据不提交 Git。

## processed

保存 PySpark 脚本生成的标准化 CSV 或 Parquet 文件。这里的文件必须能够由 `src/spark_jobs/` 中的代码重新生成。

数据来源、许可、下载日期和字段差异需要记录在本文件或独立的数据来源说明中。正式选择数据集后，应补充：

- 数据集名称与来源链接；
- 获取日期和许可；
- 原始文件名；
- 数据量和字段数量；
- 已知缺失、异常或偏差。
